services:
  - type: web
    name: niyantrit-backend
    env: python
    rootDir: niyantrit-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
